{
  "name": "selenium-automation-service",
  "version": "1.0.0",
  "description": "Runs nodejs http server and handle request to run automation.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "joechang",
  "license": "ISC",
  "dependencies": {
    "http-proxy": "^1.18.1",
    "selenium-webdriver": "^4.15.0"
  }
}

